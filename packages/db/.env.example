# Authoring/CMS database only. Never on the public request path — the read side
# ships as versioned static JSON chunks, so this can be down and the site works.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/accessprofile?schema=public"
